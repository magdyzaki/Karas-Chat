# تقرير تحليل مشكلة "Failed to fetch" عند رابط الدعوة

## المسار الكامل للطلب

```
صديقك يفتح: https://karas-chat.vercel.app/?invite=xxx
    ↓
App.jsx: عرض InvitePage
    ↓
InvitePage: prewakeBackend() → GET /api/health ✅ (يعمل)
    ↓
صديقك يضغط "انتقل إلى التطبيق"
    ↓
consumeInviteLink(token):
  1. المحاولة الأولى: POST /api/consume-invite (proxy على Vercel)
     → الـ proxy يستدعي Render: POST .../api/consume-invite/{token}
     → هنا قد يحدث الفشل ⚠️
  2. المحاولة الثانية (fallback): POST مباشرة إلى Render
     → من متصفح صديقك إلى karas-chat-backend.onrender.com
     → قد يفشل إذا شبكة شركة الاتصالات تحجب Render ⚠️
```

---

## نقاط الخلل المحتملة

### 1. انتهاء مهلة الـ Proxy (الأقوى)

**الملف:** `frontend/api/consume-invite.js`

- الـ proxy على Vercel يستدعي Render ويُنتظر الرد.
- Vercel يقطع الـ serverless بعد نحو **10 ثوانٍ** (Hobby).
- عند نوم Render، الاستجابة تحتاج حتى **50+ ثانية**.
- النتيجة: انتهاء مهلة → 504 أو "Failed to fetch" → الفشل يظهر للمستخدم.

**ملاحظة:** `/api/health` يعيد دائماً `{"ok":true}` حتى لو فشل الاستدعاء لـ Render، لأن هناك `try/catch` دون إعادة رمي الخطأ. إذن نجاح `/api/health` لا يعني أن Render يعمل.

---

### 2. ترتيب المحاولات (Proxy أولاً ثم Direct)

**الملف:** `frontend/src/api.js` – `consumeInviteLink`

- المحاولة الأولى: proxy على Vercel (مهلة 10 ثوانٍ).
- المحاولة الثانية: طلب مباشر من المتصفح إلى Render (مهلة 90 ثانية مع إعادة محاولة).

**السيناريو الحالي:**
- إذا كان Render بارداً: الـ proxy ينتهي مهلته → "Failed to fetch".
- ثم الطلب المباشر: إذا كانت شبكة صديقك تحجب Render → فشل آخر → نفس المشكلة.

**بديل:** تجربة الطلب المباشر أولاً (مهلة أطول وإعادة محاولة) ثم استخدام الـ proxy كخطة احتياطية.

---

### 3. إعدادات Vercel

**ملف:** `frontend/vercel.json`

- الـ rewrite الحالي: `"/(.*)" → "/index.html"`.
- مسارات `/api/*` يجب أن تُوجَّه إلى serverless قبل الـ rewrite.
- إذا كان Root Directory في مشروع Vercel مختلفاً عن `chat-app-new/frontend`، قد لا تُنشر مجلد `api/` أصلاً.

**ما يجب التحقق منه:**
- المشروع مرتبط بمستودع **Karas-Chat** وليس Karas-Chat-App.
- Root Directory = `chat-app-new/frontend`.

---

### 4. احتمال حجب Render من شبكة صديقك

- بعض مزودي الإنترنت أو شبكات الهاتف تحجب نطاقات مثل `*.onrender.com`.
- الطلب المباشر من متصفح صديقك إلى Render قد يفشل بسبب ذلك.
- نجاح `/api/health` لا يعني أن الطلب المباشر إلى Render سينجح، لأن `/api/health` يتم من Vercel إلى Render، وليس من متصفح صديقك.

---

## ملخص التشخيص

| # | المشكلة | التأثير | الأولوية |
|---|---------|---------|----------|
| 1 | انتهاء مهلة الـ proxy (10 ثوانٍ) أمام استجابة Render البطيئة | فشل شبه دائم عند نوم Render | عالية |
| 2 | ترتيب المحاولات (Proxy ثم Direct) | تقليل فرص نجاح الطلب المباشر | متوسطة |
| 3 | عدم التأكد من إعدادات Vercel والـ Root Directory | احتمال عدم نشر الـ API | متوسطة |
| 4 | حجب شبكة صديقك لـ Render | فشل الطلب المباشر فقط | متوسطة |

---

## التوصيات (بدون تنفيذ حالياً)

1. **تغيير ترتيب المحاولات:** محاولة الطلب المباشر إلى Render أولاً (مهلة 90 ثانية)، ثم الـ proxy كخطوة احتياطية.
2. **التأكد من Vercel:** في لوحة Vercel التحقق من Root Directory ومستودع النشر.
3. **اختبار من شبكة أخرى:** طلب من صديقك التجربة عبر شبكة واي فاي أو شبكة مختلفة.
4. **استمرار إيقاظ Render:** التأكد أن GitHub Action يعمل كل 5 دقائق ويستدعي Render.
