# الحل الشامل لمشكلة "Failed to fetch" — 24 ساعة

## التعديلات المُنفذة

### 1. إعادة المحاولة للـ Proxy (3 مرات + 5 ثوانٍ بين كل محاولة)
- المحاولة الأولى قد تفشل (Render نائم)
- الانتظار 5 ثوانٍ يُعطي Render وقتاً للاستيقاظ
- المحاولة الثانية والثالثة تزيدان فرصة النجاح

### 2. إيقاظ مكثّف عند فتح الصفحة
- 3 طلبات إلى `/api/health` بفارق 0، 2، 4 ثوانٍ
- يُسرّع استيقاظ Render قبل الضغط على الزر

### 3. GitHub Action كل دقيقة
- كان كل 5 دقائق → أصبح **كل دقيقة**
- تقليل وقت نوم Render إلى حد أدنى

---

## أوامر الرفع

```powershell
cd D:\programs\Smart_CRM_Final_Arabic\chat-app-new
.\deploy.ps1
```

**ملاحظة:** لو فشل push بسبب workflow، عدّل `.github/workflows/keep-render-awake.yml` يدوياً على GitHub وغيّر `*/5` إلى `*` (كل دقيقة).

---

## للتأكد من استقرار العمل 24 ساعة

### UptimeRobot (مجاني — يُنصح به)

1. https://uptimerobot.com → إنشاء حساب
2. Add Monitor → HTTP(s)
3. URL: `https://karas-chat-backend.onrender.com/api/health`
4. Interval: **5 دقائق**
5. حفظ

هذا يضمن بقاء Render مستيقظاً بشكل شبه مستمر.
