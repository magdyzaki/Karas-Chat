# نشر Karas شات على Render (يعمل 24/7 بدون جهازك)

Render يعطيك استضافة **مجانية** للمشروع، ويعمل من أي مكان بدون تشغيل جهازك.

---

## متطلبات

1. حساب على **Render** → [render.com](https://render.com) (سجّل بالبريد)
2. الحساب **مجاني** ولا يحتاج بطاقة ائتمان
3. مشروعك على **GitHub** (إذا لم يكن بعد، انشئ مستودعاً وارفع المشروع)

---

## خطوات النشر

### 1. إعداد مشروع GitHub

1. أنشئ مستودعاً جديداً على GitHub (مثلاً: `karas-chat`)
2. ارفع مجلد المشروع بالكامل:
   ```
   Smart_CRM_Final_Arabic/chat-app-new/  ← كل محتويات هذا المجلد
   ```
3. تأكد أن الملف `render.yaml` موجود في جذر المشروع

### 2. ربط Render بمستودع GitHub

1. ادخل إلى [dashboard.render.com](https://dashboard.render.com)
2. اضغط **New** → **Web Service**
3. اختر **Connect a repository** وربط حساب GitHub
4. اختر مستودع المشروع (مثلاً `karas-chat`)

### 3. إعداد الخدمة

- **Root Directory:** اكتب `backend`
- **Build Command:** `npm install`
- **Start Command:** `npm start`
- **Instance Type:** Free

### 4. إضافة المتغيرات (Environment Variables)

من القائمة الجانبية: **Environment** → **Add Environment Variable**

| المتغير | القيمة |
|--------|--------|
| `NODE_ENV` | `production` |
| `JWT_SECRET` | أي نص عشوائي طويل (مثل: `أكتب-هنا-كلمة-سر-قوية-وعشوائية`) |
| `APPROVAL_MODE` | `true` |
| `TRUSTED_PHONES` | أرقامك (مثل: `201229084204,01229084204`) |

(اختياري) لو استخدمت إشعارات الويب:
- `VAPID_PUBLIC_KEY`
- `VAPID_PRIVATE_KEY`

### 5. نشر الخدمة

اضغط **Create Web Service** وانتظر حتى ينتهي البناء والنشر.

---

## بعد النشر

- الرابط سيكون مثل:  
  `https://karas-chat-backend.onrender.com`

### تحديث رابط السيرفر في التطبيق

1. افتح التطبيق على الموبايل
2. من القائمة (☰) → **رابط السيرفر**
3. أدخل الرابط الجديد:  
   `https://karas-chat-backend.onrender.com`

أو في تسجيل الدخول: ضع نفس الرابط في حقل "رابط السيرفر" ثم سجّل الدخول.

---

## ملاحظات مهمة

### 1. السيرفر يتوقف بعد 15 دقيقة بدون استخدام

- على الخطة المجانية، السيرفر يتوقف بعد 15 دقيقة من عدم الاستخدام.
- عند أول طلب بعد التوقف، قد يستغرق التشغيل **حوالي دقيقة** حتى يعود الخدمة.
- هذا طبيعي ولا يحتاج أي إعداد إضافي.

### 2. البيانات (LowDB)

- البيانات تُخزَّن في القرص داخل الخدمة.
- في الخطة المجانية القرص **مؤقت**، أي أن المحادثات والمستخدمين قد تضيع عند:
  - إعادة النشر (Redeploy)
  - إعادة تشغيل الخدمة
- للاحتفاظ بالبيانات بشكل دائم يمكن لاحقاً استخدام **MongoDB Atlas** (مجاني) مع تعديلات بسيطة في الكود.

### 3. WebSocket (الشات المباشر)

- Render يدعم WebSocket.
- قد يقطع الاتصال أحياناً بعد فترة؛ التطبيق يعيد الاتصال تلقائياً عند فتح التطبيق أو تحديث الصفحة.

---

## حل المشاكل

| المشكلة | الحل |
|---------|------|
| فشل البناء | تأكد أن `Root Directory` = `backend` |
| خطأ في التشغيل | راجع الـ **Logs** في Render |
| لا يصل من التطبيق | تأكد أن رابط السيرفر في التطبيق يطابق رابط Render بدون `/` في النهاية |

---

## الخلاصة

بعد تنفيذ الخطوات، السيرفر يعمل من أي مكان ومن أي جهاز متصل بالإنترنت، بدون الحاجة لتشغيل جهازك.
