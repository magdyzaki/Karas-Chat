# chat-app-new — تشغيل محلي ونشر

---

## 1) التشغيل محلياً

### الباكند
```bash
cd D:\programs\Smart_CRM_Final_Arabic\chat-app-new\backend
npm install
npm start
```
أو للتطوير مع إعادة تحميل تلقائية:
```bash
npm run dev
```
يعمل على المنفذ **5002** (من ملف .env).

### الفرونتند
```bash
cd D:\programs\Smart_CRM_Final_Arabic\chat-app-new\frontend
npm install
npm run dev
```
يفتح على **https://localhost:5173** (أو المنفذ المعروض) مع proxy للـ API و Socket.IO.

---

## 2) التشغيل على الموبايل (ngrok)

### الخطوات
1. شغّل الباكند كما أعلاه.
2. في نافذة جديدة: `ngrok http 5002`
3. انسخ رابط ngrok (مثل `https://xxxx.ngrok-free.app`)
4. في frontend أنشئ ملف `.env`:
   ```
   VITE_API_URL=https://رابط-ngrok-هنا
   ```
5. أعد تشغيل الفرونتند: `npm run dev`
6. على الموبايل: افتح الرابط الذي يظهر (مثل `https://192.168.x.x:5173`)

---

## 3) النشر على السحابة

### الباكند (Koyeb / Render)
- ارفع مجلد `backend` إلى GitHub.
- أنشئ خدمة Web على Koyeb أو Render.
- ضبط المتغيرات: `PORT`, `JWT_SECRET`, `DB_PATH` (للتخزين الدائم)، `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`.

### الفرونتند (Vercel)
- ارفع مجلد `frontend` إلى GitHub.
- اربط بمشروع Vercel.
- ضبط `VITE_API_URL` = رابط الباكند المنشور.
- ضبط `VITE_VAPID_PUBLIC_KEY` = نفس `VAPID_PUBLIC_KEY` من الباكند (للتنبيهات).

---

## 4) ما تم إضافته

- **npm start** يقرأ ملف `.env` تلقائياً.
- **ngrok-skip-browser-warning** في طلبات الـ API لدعم ngrok المجاني.
- البيانات تُحفظ في `db.json` داخل مجلد المشروع.
